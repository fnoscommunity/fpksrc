#default file provided by monit spk

check process ZNC with pidfile /var/apps/znc/var/znc.pid
    start program = "/var/apps/znc/scripts/start-stop-status start"
    stop program  = "/var/apps/znc/scripts/start-stop-status stop"
    if failed host localhost port 8250 then restart
    if 3 restarts within 5 cycles then timeout

#end
